name: Run Swift Package test

on:
  push:

jobs:
  test:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Test
        run: |
          swift test
